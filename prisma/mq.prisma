generator client {
  provider        = "prisma-client-js"
  output = "../node_modules/@mqdb"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_MQ")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}